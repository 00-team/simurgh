type ProjectModel = {
    project_id: number
    creator: number
    name: string
    storage: string
    blogs: string
    records: number
    created_at: number
    edited_at: number
    api_key: string | null
}

export type { ProjectModel }
